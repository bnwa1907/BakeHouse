apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dep.fullname" . }}
  labels:
    {{- include "helm-dep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dep.selectorLabels" . | nindent 4 }}
